<div class="nav-bar">
    <div class="nav-bar-content wide">
        <div class="left-nav">
            {% if page.title == "Home" %}
            <a href="#top">
                {% else %}
                <a href="/">
                    {% endif %}
                    <div class=" site-name">
                        <div><img class="nav-site-image" src="{{ people.me.picture }}" alt="me"></div>
                        <div class="nav-bar-title pink-text">George Kaye</div>
                    </div>
                </a>
        </div>
        <div class="desktop right-nav">
            {% for link in nav.all-links %}
            {% include macros/nav-link link=link %}
            {% endfor %}
            {% if page.title == "Home" %}
            <div class="nav-link home-link">•</div>
            {% for link in nav.home-links %} {% include macros/nav-link %}
            {% endfor %}
            {% endif %}
            {% if page.type == "trains" %}
            {% for link in home-links %}
            {% include macros/nav-link %}
            {% endfor %}
            {% endif %}
        </div>
    </div>
    <!-- <div class="mobile mobile-nav">
        <div class="nav-link"><a href="https://github.com/georgejkaye">GitHub</a></div>
        <div class="nav-link"><a href="/blog">Blog</a></div>
    </div> -->
</div>